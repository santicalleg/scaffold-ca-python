"""{{ pascal_name }} — repository (SQLAlchemy / asyncpg) driven adapter."""
from __future__ import annotations

from {{ package }}.domain.model.gateways.{{ snake_name }}_gateway import {{ pascal_name }}Gateway


class {{ pascal_name }}Repository({{ pascal_name }}Gateway):
    """Repository adapter for {{ pascal_name }}Gateway using SQLAlchemy / asyncpg.

    Inject your database session in the constructor.
    """

    def __init__(self, session=None) -> None:
        self._session = session

    # TODO: implement gateway methods using self._session
