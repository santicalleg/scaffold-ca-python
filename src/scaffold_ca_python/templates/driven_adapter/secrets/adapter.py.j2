"""{{ pascal_name }} — secrets provider driven adapter."""
from __future__ import annotations

from {{ package }}.domain.model.gateways.{{ snake_name }}_gateway import {{ pascal_name }}Gateway


class {{ pascal_name }}SecretsAdapter({{ pascal_name }}Gateway):
    """Secrets provider adapter for {{ pascal_name }}Gateway.

    Inject a boto3 SecretsManager client or a HashiCorp Vault client.
    """

    def __init__(self, secrets_client=None) -> None:
        self._client = secrets_client

    def get_secret(self, secret_name: str) -> str:
        """Retrieve a secret value by name."""
        # TODO: implement using self._client
        raise NotImplementedError
