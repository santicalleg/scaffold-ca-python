"""{{ pascal_name }} — RabbitMQ publisher driven adapter."""
from __future__ import annotations

from {{ package }}.domain.model.gateways.{{ snake_name }}_gateway import {{ pascal_name }}Gateway


class {{ pascal_name }}RabbitMQSender({{ pascal_name }}Gateway):
    """RabbitMQ publisher adapter for {{ pascal_name }}Gateway.

    Inject a pika BlockingChannel or aio_pika Channel.
    """

    def __init__(self, channel=None, exchange: str = "") -> None:
        self._channel = channel
        self._exchange = exchange

    # TODO: implement gateway publish method(s) using self._channel
