"""{{ pascal_name }} — MongoDB driven adapter."""
from __future__ import annotations

from {{ package }}.domain.model.gateways.{{ snake_name }}_gateway import {{ pascal_name }}Gateway


class {{ pascal_name }}MongoAdapter({{ pascal_name }}Gateway):
    """MongoDB adapter for {{ pascal_name }}Gateway.

    Inject your motor AsyncIOMotorClient / pymongo MongoClient.
    """

    def __init__(self, collection=None) -> None:
        self._collection = collection

    # TODO: implement gateway methods using self._collection
