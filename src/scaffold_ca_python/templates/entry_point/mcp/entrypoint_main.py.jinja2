"""{{ name }} MCP server entrypoint."""
import asyncio

from {{ python_package }}.infrastructure.entry_points.mcp_server.server import run


def main() -> None:
    """Start the MCP server."""
    asyncio.run(run())


if __name__ == "__main__":
    main()
