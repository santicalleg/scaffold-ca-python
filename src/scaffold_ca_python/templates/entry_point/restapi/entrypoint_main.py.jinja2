"""{{ name }} REST API entrypoint."""
import uvicorn


def main() -> None:
    """Start the uvicorn server."""
    uvicorn.run(
        "{{ python_package }}.infrastructure.entry_points.restapi.main:app",
        host="0.0.0.0",
        port=8000,
        reload=False,
    )


if __name__ == "__main__":
    main()
