"""Tests for Pydantic schemas — {{ project.name }}."""
from __future__ import annotations

from pydantic import BaseModel

from {{ project.python_package }}.infrastructure.entry_points.api.v1.schemas import ExampleResponse


def test_example_response_is_pydantic_model() -> None:
    assert issubclass(ExampleResponse, BaseModel)


def test_example_response_has_message_field() -> None:
    instance = ExampleResponse(message="hello")
    assert instance.message == "hello"
